<% content_for(:title) { "#{@category.name} - #{current_user.username}" } %>

<%= render 'errors' %>

<div class="row">
  <div class="<%= col(6) %>">
    <%= render 'list' %>
  </div>
  <div class="<%= col(6) %>">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title"><%= t('title.categories.edit') %></h3>
      </div>
      <div class="panel-body">
        <%= render 'form' %>
      </div>
    </div>
  </div>
</div>

